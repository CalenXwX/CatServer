--- a/net/minecraft/world/level/block/DropperBlock.java
+++ b/net/minecraft/world/level/block/DropperBlock.java
@@ -15,8 +_,14 @@
 import net.minecraft.world.level.block.state.BlockBehaviour;
 import net.minecraft.world.level.block.state.BlockState;
 
+// CraftBukkit start
+import net.minecraft.world.CompoundContainer;
+import org.bukkit.craftbukkit.v1_20_R1.inventory.CraftItemStack;
+import org.bukkit.event.inventory.InventoryMoveItemEvent;
+// CraftBukkit end
+
 public class DropperBlock extends DispenserBlock {
-   private static final DispenseItemBehavior f_52939_ = new DefaultDispenseItemBehavior();
+   private static final DispenseItemBehavior f_52939_ = new DefaultDispenseItemBehavior(true); // CraftBukkit
 
    public DropperBlock(BlockBehaviour.Properties p_52942_) {
       super(p_52942_);
@@ -30,7 +_,7 @@
       return new DropperBlockEntity(p_153179_, p_153180_);
    }
 
-   protected void m_5824_(ServerLevel p_52944_, BlockPos p_52945_) {
+   public void m_5824_(ServerLevel p_52944_, BlockPos p_52945_) { // CatServer - protected -> public
       BlockSourceImpl blocksourceimpl = new BlockSourceImpl(p_52944_, p_52945_);
       DispenserBlockEntity dispenserblockentity = blocksourceimpl.m_8118_();
       int i = dispenserblockentity.m_222761_(p_52944_.f_46441_);
@@ -38,15 +_,39 @@
          p_52944_.m_46796_(1001, p_52945_, 0);
       } else {
          ItemStack itemstack = dispenserblockentity.m_8020_(i);
-         if (!itemstack.m_41619_()) {
+         if (!itemstack.m_41619_() && net.minecraftforge.items.VanillaInventoryCodeHooks.dropperInsertHook(p_52944_, p_52945_, dispenserblockentity, i, itemstack)) {
             Direction direction = p_52944_.m_8055_(p_52945_).m_61143_(f_52659_);
             Container container = HopperBlockEntity.m_59390_(p_52944_, p_52945_.m_121945_(direction));
             ItemStack itemstack1;
             if (container == null) {
+               // Fabric start - DropperBlockMixin
+               {
+                  org.spongepowered.asm.mixin.injection.callback.CallbackInfo ci = new org.spongepowered.asm.mixin.injection.callback.CallbackInfo("fabric$hookDispense", true);
+                  this.fabric$hookDispense(p_52944_, p_52945_, ci);
+                  if (ci.isCancelled()) {
+                     return;
+                  }
+               }
+               // Fabric end
                itemstack1 = f_52939_.m_6115_(blocksourceimpl, itemstack);
             } else {
-               itemstack1 = HopperBlockEntity.m_59326_(dispenserblockentity, container, itemstack.m_41777_().m_41620_(1), direction.m_122424_());
-               if (itemstack1.m_41619_()) {
+               // CraftBukkit start - Fire event when pushing items into other inventories
+               CraftItemStack oitemstack = CraftItemStack.asCraftMirror(itemstack.m_41777_().m_41620_(1));
+               org.bukkit.inventory.Inventory destinationInventory;
+               // Have to special case large chests as they work oddly
+               if (container instanceof CompoundContainer) {
+                  destinationInventory = new org.bukkit.craftbukkit.v1_20_R1.inventory.CraftInventoryDoubleChest((CompoundContainer) container);
+               } else {
+                  destinationInventory = catserver.server.inventory.CatInventoryUtils.getOwner(container).getInventory(); // CatServer
+               }
+               InventoryMoveItemEvent event = new InventoryMoveItemEvent(dispenserblockentity.getOwner().getInventory(), oitemstack.clone(), destinationInventory, true);
+               p_52944_.getCraftServer().getPluginManager().callEvent(event);
+               if (event.isCancelled()) {
+                  return;
+               }
+               itemstack1 = HopperBlockEntity.m_59326_(dispenserblockentity, container, CraftItemStack.asNMSCopy(event.getItem()), direction.m_122424_());
+               if (event.getItem().equals(oitemstack) && itemstack1.m_41619_()) {
+                  // CraftBukkit end
                   itemstack1 = itemstack.m_41777_();
                   itemstack1.m_41774_(1);
                } else {
@@ -58,4 +_,34 @@
          }
       }
    }
+
+   // Fabric start - DropperBlockMixin
+   public void fabric$hookDispense(ServerLevel p_52944_, BlockPos p_52945_, org.spongepowered.asm.mixin.injection.callback.CallbackInfo ci) {
+      DispenserBlockEntity dispenser = (DispenserBlockEntity) p_52944_.m_7702_(p_52945_);
+      Direction direction = dispenser.m_58900_().m_61143_(DispenserBlock.f_52659_);
+
+      net.fabricmc.fabric.api.transfer.v1.storage.Storage<net.fabricmc.fabric.api.transfer.v1.item.ItemVariant> target = net.fabricmc.fabric.api.transfer.v1.item.ItemStorage.SIDED.find(p_52944_, p_52945_.m_121945_(direction), direction.m_122424_());
+
+      if (target != null) {
+         // Always cancel if a storage is available.
+         ci.cancel();
+
+         // We pick a non empty slot. It's not necessarily the same as the one vanilla picked, but that doesn't matter.
+         int slot = dispenser.m_222761_(p_52944_.f_46441_);
+
+         if (slot == -1) {
+            net.fabricmc.fabric.impl.transfer.TransferApiImpl.LOGGER.warn("Skipping dropper transfer because the empty slot is unexpectedly -1.");
+            return;
+         }
+
+         net.fabricmc.fabric.api.transfer.v1.storage.StorageUtil.move(
+                 net.fabricmc.fabric.api.transfer.v1.item.InventoryStorage.of(dispenser, null).getSlot(slot),
+                 target,
+                 k -> true,
+                 1,
+                 null
+         );
+      }
+   }
+   // Fabric end
 }
